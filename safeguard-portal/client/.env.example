# Copy to .env and adjust. Vite only exposes variables prefixed with VITE_.

VITE_API_BASE_URL=http://localhost:5001
# Set to the clientid printed by `node server/createClient.js` (dev only; overrides stale login clientId).
# VITE_DEV_CLIENT_ID=6
